import { NoCopyPasteDirective } from './no-copy-paste.directive';

describe('NoCopyPasteDirective', () => {
  it('should create an instance', () => {
    const directive = new NoCopyPasteDirective();
    expect(directive).toBeTruthy();
  });
});
